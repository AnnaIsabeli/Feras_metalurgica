# Local development only. Copy to .env and change the password before running.
POSTGRES_DB=fera
POSTGRES_USER=fera
POSTGRES_PASSWORD=change-this-local-password
APP_ENV=development
DEV_AUTH_ENABLED=true
CORS_ORIGIN=http://localhost:5000
